# chipyard-docker
Docker container for Chipyard development

## Installation
Build the image by running this in the root directory:
```sh
docker compose build
```

## running container

```sh
docker compose up -d
docker compose exec chipyard sh
```


